#!/bin/sh
# Template for setting up local Git configuration.
# Replace the placeholders with your own details.

# Set your Git user email (replace with your own email address).
git config --global user.email 'your-email@example.com'

# Set your preferred default branch name.
git config --global init.defaultBranch main

# Add any additional Git configuration commands below.
# Example: git config --global user.name 'Your Name'
