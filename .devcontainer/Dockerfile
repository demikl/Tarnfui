# Use the base Python dev container image
FROM mcr.microsoft.com/devcontainers/python:3.13

# Install additional tools or dependencies if needed
# Example: RUN apt-get update && apt-get install -y <package>
